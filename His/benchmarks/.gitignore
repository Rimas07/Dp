# Результаты тестов (не коммитим каждый раз)
comparison-results.json
benchmark-results.json

# Сохраненные результаты с датами (можно коммитить важные)
# results-*.json

# Временные файлы
*.tmp
*.log
