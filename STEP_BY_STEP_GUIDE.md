# 📚 ПОШАГОВАЯ ИНСТРУКЦИЯ ПО ДОРАБОТКЕ ПРОЕКТА

## ✅ ШАГ 1: ИСПРАВЛЕНИЕ RABBITMQ (ЗАВЕРШЕНО)

### Что было сделано:

1. ✅ Создан `.env` файл в `His/` с переменными окружения
2. ✅ Создан `.env` файл в `consumer/`
3. ✅ Обновлён `His/src/audit/audit.module.ts` - использует RABBITMQ_URL из .env
4. ✅ Обновлён `consumer/src/main.ts` - использует RABBITMQ_URL из .env
5. ✅ Обновлён `His/docker-compose.yml`:
   - Добавлен RABBITMQ_URL в his-app
   - Добавлен health check для RabbitMQ
   - Убран `profiles` из consumer (теперь запускается автоматически)

### Как проверить что работает:

```bash
cd /home/user/Dp/His

# 1. Остановить старые контейнеры
docker-compose down

# 2. Пересобрать и запустить
docker-compose up --build

# 3. Проверить что все сервисы запущены
docker-compose ps

# Должны быть запущены:
# - his-app (порт 3000)
# - mongodb (порт 27017)
# - rabbitmq (порты 5672, 15672)
# - consumer (без портов, работает в фоне)
```

### Проверка логов Consumer:

```bash
# Смотрим логи consumer
docker-compose logs -f consumer

# Должны увидеть:
# ✅ [Consumer] Successfully connected to RabbitMQ and listening for messages...
```

### Проверка RabbitMQ Management UI:

Откройте в браузере: http://localhost:15672
- Login: admin
- Password: admin123

Проверьте:
1. В разделе "Queues" должна быть очередь `audit-queue`
2. В разделе "Connections" должны быть подключения от his-app и consumer

### Проверка работы аудита:

```bash
# Отправьте любой запрос к API (например создайте tenant)
# Затем проверьте логи consumer:

docker-compose logs consumer | grep "AUDIT"

# Должны увидеть логи типа:
# [AUDIT] {"timestamp":"2025-11-07T...", "tenantId":"...", ...}
```

---

## 📊 ШАГ 2: ДОБАВЛЕНИЕ МЕТРИК (Следующий шаг)

### Зачем нужны метрики?

Для диплома вам нужны **графики и статистика**:
- Количество запросов через прокси
- Использование лимитов по tenant
- Время ответа (latency)
- Количество ошибок

### Что будем делать:

1. Установим пакет для метрик
2. Создадим MetricsService
3. Добавим счётчики в прокси и лимиты
4. Создадим endpoint `/metrics` для получения данных

### Время: 2-3 часа

---

## 🧪 ШАГ 3: НАПИСАНИЕ ТЕСТОВ (После метрик)

### Зачем нужны тесты?

**На защите диплома будут вопросы:**
- "Как вы тестировали систему?"
- "Какой coverage у вас?"
- "Покажите результаты тестов"

**БЕЗ ТЕСТОВ ОЧЕНЬ СЛОЖНО ЗАЩИТИТЬСЯ!**

### Что будем тестировать:

#### Unit тесты (3-4 часа):
1. `limits.service.spec.ts` - проверка атомарных операций
2. `proxy.service.spec.ts` - проверка валидации запросов
3. `auth.service.spec.ts` - проверка JWT токенов
4. `tenants.service.spec.ts` - проверка создания tenant

#### E2E тесты (2-3 часа):
1. Создание tenant через API
2. Проверка превышения лимитов
3. Прокси запрос через /mongo/*
4. Проверка изоляции данных между tenant

### Время: 5-7 часов

---

## 🐳 ШАГ 4: ОПТИМИЗАЦИЯ DOCKER (После тестов)

### Зачем?

- Уменьшить размер образов (сейчас ~1GB, можно ~200MB)
- Ускорить сборку
- Использовать best practices

### Что будем делать:

1. Использовать multi-stage build
2. Использовать Alpine образ
3. Кэшировать зависимости

### Время: 1 час

---

## 📖 ШАГ 5: ДОКУМЕНТАЦИЯ (Последний шаг)

### Что создадим:

1. **README.md** - полная инструкция по запуску
2. **ARCHITECTURE.md** - описание архитектуры с диаграммами
3. **API_EXAMPLES.md** - примеры использования API

### Время: 2 часа

---

## 📅 РЕКОМЕНДУЕМЫЙ ГРАФИК:

### День 1 (сегодня):
- ✅ ШАГ 1: RabbitMQ (СДЕЛАНО)
- ⏱️ ШАГ 2: Метрики (2-3 часа)

### День 2:
- ⏱️ ШАГ 3: Unit тесты (4 часа)
- ⏱️ ШАГ 4: Docker оптимизация (1 час)

### День 3:
- ⏱️ ШАГ 3: E2E тесты (3 часа)
- ⏱️ ШАГ 5: Документация (2 часа)

### День 4-7:
- 📝 Написание Word документа
- 📊 Создание графиков из метрик
- 🎯 Подготовка презентации

---

## ❓ ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ:

### Q: Нужно ли останавливать Docker каждый раз?

A: Нет, только когда меняете docker-compose.yml или Dockerfile. Для изменений в коде достаточно пересобрать:

```bash
docker-compose up --build his-app
```

### Q: Как посмотреть логи всех сервисов?

```bash
# Все логи
docker-compose logs -f

# Только his-app
docker-compose logs -f his-app

# Только consumer
docker-compose logs -f consumer
```

### Q: Как очистить всё и начать заново?

```bash
# Остановить и удалить контейнеры
docker-compose down

# Удалить volumes (база данных!)
docker-compose down -v

# Удалить образы
docker-compose down --rmi all

# Запустить заново
docker-compose up --build
```

---

## 🎯 ГОТОВ ПРОДОЛЖИТЬ?

Когда будете готовы к **ШАГу 2 (Метрики)**, дайте знать!

Я создам:
1. MetricsService с счётчиками
2. Endpoint `/metrics` для получения статистики
3. Интеграцию с прокси и лимитами
4. Примеры графиков для Word документа
